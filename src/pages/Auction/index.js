import Header from 'components/Header';

const Auction = () => (
  <div className="container-fluid">
    <Header />
    <div className="layout mt-4 main">
      auction page
    </div>
  </div>
);

export default Auction;

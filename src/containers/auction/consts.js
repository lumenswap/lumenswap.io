export const statusClassNames = (styles) => ({
  live: styles['status-live'],
  ended: styles['status-ended'],
  'not started': styles['status-not-started'],
});

export const STATUS_NAMES = {
  live: 'Live',
  ended: 'Ended',
  'not-started': 'Not started',
};
